{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Build mini_shell",
            "command": "make",
            "args": [
                "all"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Build whole project with Makefile"
        }
    ]
}